import {useEffect,useState} from 'react';
import {useNavigate} from 'react-router-dom';
import {supabase} from '../lib/supabase';
import {publicMediaUrl} from '../lib/productMedia';
export default function ShopCategories(){const nav=useNavigate(),[cats,setCats]=useState([]);useEffect(()=>{supabase.from('product_categories').select('id,name,image_path').eq('is_active',true).order('name').then(({data})=>setCats(data||[]))},[]);return <div className="min-h-screen bg-bg text-text" dir="rtl"><header className="sticky top-0 z-30 border-b border-border bg-card/95 p-4"><div className="mx-auto flex max-w-7xl items-center justify-between"><button onClick={()=>nav('/')} className="text-xl font-black text-accent">iShop</button><button onClick={()=>nav('/')} className="text-sm font-black">← الرئيسية</button></div></header><main className="mx-auto max-w-7xl px-4 py-6"><h1 className="mb-1 text-2xl font-black">كل الأقسام</h1><p className="mb-5 text-xs text-muted">اختر القسم لعرض المنتجات المتاحة بالمخزون</p><div className="grid grid-cols-2 gap-3 sm:grid-cols-4 lg:grid-cols-6">{cats.map(c=><button key={c.id} onClick={()=>nav(`/products?category=${c.id}`)} className="rounded-2xl border border-border bg-card p-3"><div className="aspect-square overflow-hidden rounded-xl bg-surface">{c.image_path?<img src={publicMediaUrl('category-images',c.image_path)} alt={c.name} className="size-full object-cover"/>:<div className="grid size-full place-items-center text-4xl opacity-30">▦</div>}</div><b className="mt-3 block text-sm">{c.name}</b></button>)}</div></main></div>}